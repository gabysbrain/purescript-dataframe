{
  "name": "purescript-dataframe",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "benchmark": "pulp build -I benchmarks --main Benchmarks.Main -O --to output/benchmarks.js && (echo '*' | node output/benchmarks.js)",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "benchmark": "^2.1.3",
    "bower": "^1.8.0",
    "pulp": "^10.0.0"
  }
}
